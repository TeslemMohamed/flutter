name: mahadre
description: تطبيق متابعة المحاظر القرآنية

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # ========== FIREBASE PACKAGES ==========
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_storage: ^13.0.5
  firebase_messaging: ^16.1.0
  
  # ========== STATE MANAGEMENT ==========
  provider: ^6.0.5
  flutter_bloc: ^9.1.1  # أو أي state management تفضله
  
  # ========== UI & UTILITIES ==========
  intl: ^0.20.2
  flutter_svg: ^2.0.9
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  flutter_localizations:
    sdk: flutter
  
  # ========== FORMS & VALIDATION ==========
  flutter_form_builder: ^10.2.0
  
  # ========== CHARTS ==========
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0